{# @var form \BootstrapForm\Form #}
{# @var config \BootstrapForm\FieldConfig #}
{# @var field string #}
{% if not form.IsSuccess() %}
    <input type="submit" name="{{ actionId }}submit" value="{{ config.display.label }}" class="btn btn-primary">
{% endif %}